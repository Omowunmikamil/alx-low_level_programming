#include "main.h"

/**
 * _isupper - checks for uppercase character
 * Return: 1 if c is uppercase, otherwise return 0
*/

int _isupper(int c);
{
	int c;

	if (c >= '0' && c <= '9')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
