#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - print alphabet in lowercase
 *
 * Return: 0
*/

int main(void)

	for (char ch = 'a'; ch <= 'z'; ch++)
		putchar(ch);
		putchar('\n');
	return (0);
