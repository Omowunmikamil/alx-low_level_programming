#include "main.h"

/**
 * print_last_digit -  prints the last digit of a number
 * @n: argument been passed
 * Return: the value of last digit
*/

int print_last_digit(int n)
{

	int number = n % 10;

	if (num < 0)
	{
		number *= -1;
	}
	_putchar(number + '0');
	return (0);
}
